# Local/dev values
PORT=4001

# FRONTEND_URL should be your production frontend domain (e.g., https://app.example.com)
FRONTEND_URL=http://localhost:5173

# Use MongoDB Atlas in production. LOCAL TEST PURPOSES ONLY.
# IMPORTANT: do NOT commit real credentials. Set the real DB_URL on your host provider.
# Replace <PASSWORD> and <DBNAME> with actual values when setting env on the host.
DB_URL=mongodb+srv://job_portal:<PASSWORD>@cluster0.htyu6k2.mongodb.net/<DBNAME>?retryWrites=true&w=majority

# Security / runtime
JWT_SECRET_KEY=fgjfgsudgfudnhfousidfnewuikfmlewf
JWT_EXPIRE=7d
COOKIE_EXPIRE=7

# NODE_ENV: set to 'production' on the host
NODE_ENV=development

# Email provider: in production set real provider (sendgrid, mailgun, smtp) and credentials
EMAIL_PROVIDER=development
TEST_EMAIL=test@example.com
DEBUG_EMAILS=true

# -------------------------
# PRODUCTION NOTES (do NOT commit secrets)
# On your host (Render/Heroku/Vercel/Railway) set:
# NODE_ENV=production
# DB_URL=mongodb+srv://job_portal:Dharshni@cluster0.htyu6k2.mongodb.net/?appName=Cluster0
# FRONTEND_URL=http://localhost:5173
# JWT_SECRET_KEY=fgjfgsudgfudnhfousidfnewuikfmlewf
# EMAIL_PROVIDER=sendgrid
# 
# DEBUG_EMAILS=false
# PORT - use host provided port (most hosts set this automatically)
# -------------------------
# PRODUCTION NOTES:
# - On your host set DB_URL to the same connection string via the provider UI.
# - Add NODE_ENV=production, JWT_SECRET_KEY=<strong-secret>, FRONTEND_URL=https://your-frontend-domain
# - In Atlas: confirm the DB user and IP whitelist (you already updated IPs).
# - Remove or ignore this .env before pushing to a public repo.
